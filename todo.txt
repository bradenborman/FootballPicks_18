To Do list


Finish log in
Make display leaderboard script
Make admin page
Make grading scripts


Scroll spy to track week selcted or javascript to highltight week winner from dropdown


//when modal opens
$('#yourModal').on('shown.bs.modal', function (e) {
  $("#pageContent").css({ opacity: 0.5 });
})

//when modal closes
$('#yourModal').on('hidden.bs.modal', function (e) {
  $("#pageContent").css({ opacity: 1 });
})


*********************************************************
PASSWORD STORAGE 


$password = "asdf123";
$secret = password_hash($password, PASSWORD_BCRYPT);
//Store $secret in database

if (password_verify($password_entered, $stored_secret))
{ 
  //Password OK
}
else
{
  //Password not OK
}
